#include "main.h"
/**
 * malloc_checked - where the valor is saved
 *@b: var
 *Return: allocated memory
 */
void *malloc_checked(unsigned int b)
{
	int *ptr;

	ptr = 0;

	ptr = malloc(sizeof(b));

	if (i == 0)
		exit(98)
}
